# 🚨 Срочное исправление двух проблем

## Проблема 1: ❌ Invalid Server Actions request

**Причина**: Next.js в GitHub Codespaces конфликтует с proxy headers.

**Исправление уже применено**:
- Удален Server Action `getFirebaseVapidKey()`
- VAPID ключ теперь загружается напрямую из `process.env`
- Настроен `next.config.mjs`

**Ошибка больше не должна появляться после перезапуска dev сервера.**

---

## Проблема 2: ❌ Could not find the table 'public.profiles'

**Причина**: Таблицы не созданы в базе данных Supabase.

### ⚡ БЫСТРОЕ РЕШЕНИЕ:

1. **Откройте Supabase SQL Editor**:
   https://supabase.com/dashboard/project/bwgzkqnnubawzvuxijjf/sql

2. **Скопируйте весь скрипт** из файла:
   [`scripts/QUICK_FIX.sql`](scripts/QUICK_FIX.sql)

3. **Вставьте в SQL Editor и нажмите "Run"**

4. **Создайте Storage bucket** (если еще не создан):
   - Откройте: Storage → Buckets
   - Create bucket
   - Name: `avatars`
   - ✅ Public bucket: **Yes** (обязательно!)
   - Create bucket

5. **Обновите браузер** (Ctrl+F5)

---

## ✅ После исправления

Вы должны увидеть:
- ✅ Профиль загружается без ошибок
- ✅ Можно добавлять дни рождения
- ✅ Можно загружать фото профиля
- ✅ Нет ошибок в консоли

---

## 🔍 Проверка что всё работает

Выполните в Supabase SQL Editor:

```sql
-- Должно вернуть 3 таблицы
SELECT table_name FROM information_schema.tables
WHERE table_schema = 'public'
AND table_name IN ('birthdays', 'profiles', 'settings')
ORDER BY table_name;

-- Должно показать ваш профиль (после входа)
SELECT * FROM public.profiles WHERE id = auth.uid();
```

Если запросы вернули результаты - всё настроено правильно! 🎉
