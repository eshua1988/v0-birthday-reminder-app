# Настройка веб push-уведомлений для мобильного телефона

## 🎯 Как это работает

Приложение автоматически отправляет push-уведомления на телефон в точное время, заданное для каждого именинника:

1. **Установите время уведомления** для каждого именинника (например, 09:00)
2. **Разрешите уведомления** в браузере на телефоне
3. **Получайте push-уведомления** точно в назначенное время в день рождения

---

## 📱 Быстрый старт для пользователей

### Шаг 1: Разрешить уведомления на телефоне

#### Android Chrome:
1. Откройте приложение в Chrome на телефоне
2. Нажмите на **замок/информацию** слева от адресной строки
3. Найдите "**Уведомления**" (Notifications)
4. Выберите "**Разрешить**" (Allow)
5. Перезагрузите страницу

#### iOS Safari (требуется iOS 16.4+):
1. Откройте приложение в Safari
2. Нажмите значок **AA** в адресной строке
3. Выберите "**Настройки веб-сайта**"
4. Включите "**Уведомления**"

### Шаг 2: Проверить подключение

1. Перейдите в **Настройки** (боковое меню)
2. Прокрутите до секции "**Firebase Cloud Messaging**"
3. Вы должны увидеть:
   - ✅ **FCM токен** (длинная строка) - значит всё подключено
   - 🔴 Если токена нет - вернитесь к Шагу 1

### Шаг 3: Настроить время уведомлений

#### Глобальное время (для всех именинников сразу):
1. Откройте **Настройки** → секция "**Уведомления**"
2. Включите глобальное время (переключатель)
3. Установите время, например **09:00**
4. Все именинники будут использовать это время

#### Индивидуальное время (для конкретного человека):
1. Откройте карточку именинника
2. Включите "**Включить оповещение**" ✓
3. Установите время, например **13:30**
4. Нажмите "**Сохранить**"

### Шаг 4: Тестирование

1. В **Настройках** нажмите кнопку "**Отправить тестовое уведомление**"
2. Если всё настроено правильно - получите push-уведомление на телефон
3. Если нет - проверьте консоль браузера (Developer Tools)

---

## 🔧 Настройка для разработчиков

### Требования

1. **Firebase проект** с включенным Cloud Messaging (FCM)
2. **Supabase база данных** с таблицей `fcm_tokens`
3. **Vercel аккаунт** для хостинга и cron jobs

### Переменные окружения

Добавьте в Vercel Project Settings → Environment Variables:

#### 1. Firebase Client (публичные):
```env
NEXT_PUBLIC_FIREBASE_API_KEY=AIzaSy...
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abc123
NEXT_PUBLIC_FIREBASE_MEASUREMENT_ID=G-ABC123
NEXT_PUBLIC_FIREBASE_VAPID_KEY=BN5x...
```

#### 2. Firebase Admin (приватный):
```env
FIREBASE_SERVICE_ACCOUNT_KEY={"type":"service_account","project_id":"...","private_key":"-----BEGIN PRIVATE KEY-----\n..."}
```

**Где взять Service Account Key:**
1. [Firebase Console](https://console.firebase.google.com/) → Ваш проект
2. Project Settings ⚙️ → Service Accounts
3. "**Generate new private key**"
4. Скачается JSON файл
5. Скопируйте **весь JSON** в переменную `FIREBASE_SERVICE_ACCOUNT_KEY`

#### 3. Cron Secret:
```env
CRON_SECRET=your-secret-key-for-cron-auth
```

Сгенерируйте случайный ключ (например, через `openssl rand -base64 32`)

### Настройка Vercel Cron

Файл `vercel.json` уже настроен для запуска каждую минуту:

```json
{
  "crons": [
    {
      "path": "/api/cron/check-birthdays",
      "schedule": "* * * * *"
    }
  ]
}
```

**Cron формат:** `* * * * *` = каждую минуту
- Минута: `*` (0-59)
- Час: `*` (0-23)
- День месяца: `*` (1-31)
- Месяц: `*` (1-12)
- День недели: `*` (0-7)

**Примеры расписаний:**
- `*/5 * * * *` - каждые 5 минут
- `0 9 * * *` - каждый день в 09:00
- `0 9,12,18 * * *` - в 9:00, 12:00 и 18:00

### Архитектура системы

```
┌─────────────────────────────────────────────────┐
│           Пользователь (телефон)                │
│                                                  │
│  1. Открывает приложение в браузере             │
│  2. Разрешает уведомления                       │
│  3. Firebase SDK получает FCM токен             │
│  4. Токен сохраняется в БД: fcm_tokens          │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│         Vercel Cron Job (каждую минуту)         │
│                                                  │
│  /api/cron/check-birthdays                      │
│                                                  │
│  1. Получает текущее время (HH:MM:SS)           │
│  2. Ищет дни рождения на сегодня                │
│  3. Фильтрует по notification_enabled=true      │
│  4. Проверяет совпадение notification_time      │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│         Firebase Admin SDK (сервер)             │
│                                                  │
│  1. Получает FCM токены из БД                   │
│  2. Отправляет multicast сообщения              │
│  3. Обрабатывает ответы (success/failure)       │
│  4. Удаляет неактивные токены                   │
└─────────────────────────────────────────────────┘
                      ↓
┌─────────────────────────────────────────────────┐
│      Firebase Cloud Messaging (Google)          │
│                                                  │
│  Доставляет push-уведомления на устройство      │
└─────────────────────────────────────────────────┘
```

### Структура базы данных

#### Таблица `fcm_tokens`
```sql
CREATE TABLE fcm_tokens (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  token text NOT NULL UNIQUE,
  created_at timestamptz DEFAULT now(),
  updated_at timestamptz DEFAULT now()
);
```

#### Таблица `birthdays`
```sql
CREATE TABLE birthdays (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  first_name text NOT NULL,
  last_name text NOT NULL,
  birth_date date NOT NULL,
  notification_time time DEFAULT '09:00:00',
  notification_enabled boolean DEFAULT false,
  created_at timestamptz DEFAULT now()
);
```

#### Таблица `settings`
```sql
CREATE TABLE settings (
  id uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id uuid REFERENCES auth.users(id) ON DELETE CASCADE,
  key text NOT NULL,
  value text,
  UNIQUE(user_id, key)
);
```

### Проверка работы cron job

#### Локально (тестирование):
```bash
curl -X GET http://localhost:3000/api/cron/check-birthdays \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

#### В продакшене (Vercel):
```bash
curl -X GET https://your-app.vercel.app/api/cron/check-birthdays \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

**Ожидаемый ответ:**
```json
{
  "success": true,
  "currentTime": "13:53:00",
  "birthdays": 5,
  "notificationsSent": 3,
  "failures": 0
}
```

### Мониторинг логов

1. Откройте Vercel Dashboard
2. Выберите проект → Deployments
3. Кликните на последний деплой
4. Перейдите в Functions → `/api/cron/check-birthdays`
5. Смотрите логи в реальном времени

**Пример логов:**
```
[v0] Cron: Checking birthdays at: 13:53:00 Date: 2024-12-21
[v0] Cron: Found 84 birthdays with notifications enabled
[v0] Cron: Checking Иван Иванов
[v0] Cron: Sending notification for: Иван Иванов to 2 devices
[v0] Cron: FCM sent successfully
```

---

## 🐛 Устранение неполадок

### Проблема: Уведомления не приходят

#### Чеклист для проверки:

1. **Разрешения браузера:**
   - Статус: `Notification.permission === "granted"`
   - Проверьте в консоли: `console.log(Notification.permission)`

2. **FCM токен:**
   - Откройте Настройки → Firebase Cloud Messaging
   - Должен быть длинный токен (150+ символов)
   - Если нет → перезагрузите страницу с разрешенными уведомлениями

3. **Переменные окружения Vercel:**
   ```bash
   ✓ FIREBASE_SERVICE_ACCOUNT_KEY (JSON)
   ✓ CRON_SECRET (строка)
   ✓ NEXT_PUBLIC_FIREBASE_* (все 8 переменных)
   ```

4. **Настройки именинника:**
   - ✓ Дата рождения = сегодня (день/месяц)
   - ✓ Время уведомления = текущее время (±1 минута)
   - ✓ notification_enabled = true
   - ✓ Формат времени: `HH:MM:00` (с секундами)

5. **Cron job запущен:**
   - В Vercel Dashboard → Settings → Cron Jobs
   - Должна быть запись: `/api/cron/check-birthdays` со статусом Active

### Проблема: FCM токен не сохраняется

**Причины:**
- Firebase переменные не настроены
- Service Worker не зарегистрирован
- Разрешения не даны

**Решение:**
1. Проверьте консоль браузера на ошибки
2. Убедитесь что файл `/firebase-messaging-sw.js` существует
3. Проверьте Network tab - должен быть запрос к Firebase Messaging

### Проблема: Cron job не срабатывает

**Причины:**
- Не задеплоен `vercel.json` с cron конфигурацией
- `CRON_SECRET` не настроен

**Решение:**
1. Убедитесь что `vercel.json` коммитнут и задеплоен
2. Проверьте логи в Vercel Functions
3. Вручную вызовите endpoint с `Authorization` заголовком

### Проблема: "Invalid registration token"

**Причины:**
- Токен устарел или удален пользователем
- Приложение переустановлено
- Очищены данные браузера

**Решение:**
Система автоматически удаляет неактивные токены. Пользователю нужно:
1. Зайти в приложение снова
2. Разрешить уведомления
3. Новый токен создастся автоматически

---

## 📊 Тестирование

### 1. Тестовое уведомление (быстрый тест):
```typescript
// В настройках приложения
onClick={async () => {
  const response = await fetch("/api/send-notification", {
    method: "POST",
    body: JSON.stringify({
      userId: session.user.id,
      title: "🎂 Тестовое уведомление",
      body: "Если видите это - система работает!"
    })
  });
}}
```

### 2. Локальный тест cron job:
```bash
# Создайте тестового именинника с birth_date = сегодня
# Установите notification_time на текущее время +2 минуты
# Подождите 2 минуты и проверьте - должно прийти уведомление
```

### 3. Проверка через curl:
```bash
# Замените CRON_SECRET на ваш ключ
curl -X GET https://your-app.vercel.app/api/cron/check-birthdays \
  -H "Authorization: Bearer YOUR_CRON_SECRET"
```

---

## 🚀 Деплой в продакшен

### Шаг 1: Подготовка

1. Убедитесь что все переменные окружения настроены в Vercel
2. Коммитните `vercel.json` с cron конфигурацией
3. Пушьте в GitHub

### Шаг 2: Автоматический деплой

Vercel автоматически:
- Задеплоит новую версию
- Настроит cron job по расписанию из `vercel.json`
- Запустит функцию `/api/cron/check-birthdays` каждую минуту

### Шаг 3: Проверка

1. Откройте Vercel Dashboard → Ваш проект
2. Settings → Cron Jobs
3. Должна быть запись:
   - Path: `/api/cron/check-birthdays`
   - Schedule: `* * * * *`
   - Status: **Active** ✅

### Шаг 4: Мониторинг

- Первую неделю следите за логами в Vercel Functions
- Проверяйте что уведомления приходят вовремя
- Следите за ошибками Invalid Token (удаляются автоматически)

---

## 📝 Резюме

### Что реализовано:

✅ **Cron job** проверяет дни рождения каждую минуту  
✅ **Firebase Admin SDK** отправляет push-уведомления  
✅ **FCM токены** автоматически сохраняются при первом входе  
✅ **Время уведомлений** настраивается глобально и для каждого именинника  
✅ **Тестовые уведомления** для быстрой проверки  
✅ **Автоматическая очистка** неактивных токенов  
✅ **Поддержка мультиустройств** (один пользователь = несколько токенов)  

### Что нужно сделать:

1. Настроить Firebase переменные в Vercel
2. Задеплоить `vercel.json` с cron конфигурацией
3. Разрешить уведомления на мобильных устройствах
4. Установить время для именинников

**Всё! Система будет работать автоматически 24/7** 🎉
